function [sample] = IHdistribution(n)
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here
result = zeros(n)
for i=1:n
    result(n) = rand(1,power(10,6));
    sample = sample + result(n) ;
end
end